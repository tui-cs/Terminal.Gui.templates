<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- TEMP: 2.5.0 preview vendored from tui-cs/Terminal.Gui PR #5416 (see /nuget.config);
         swap to the public 2.5.x package when it ships on nuget.org. -->
    <PackageReference Include="Terminal.Gui" Version="2.5.0-tig-remove-cm-followup.2" />
  </ItemGroup>

</Project>
